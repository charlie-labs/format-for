Here is a fenced code block that mentions +++ but should not create a details block:

```
+++ Inside code fence
console.log('+++ Also inside code');
```

And here is a real collapsible:

+++ Outside Details

This is the collapsible body.

+++
